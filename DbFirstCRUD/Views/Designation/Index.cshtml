@* @model IEnumerable<DbFirstCRUD.Data.Entities.Designation>
<h1>Designations</h1>

<table class="table">
    <thead>
        <tr>
            <th>Designation ID</th>
            <th>Designation Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var designation in Model)
        {
            <tr>
                <td>@designation.DesignationId</td>
                <td>@designation.DesignationName</td>
                <td>
                    <a asp-action="Details" asp-route-id="@designation.DesignationId">View</a>
                    <a asp-action="Edit" asp-route-id="@designation.DesignationId">Edit</a>
                    <a asp-action="Delete" asp-route-id="@designation.DesignationId" onclick="return confirm('Are you sure you want to delete this designation?');">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" class="btn btn-primary">Add New Designation</a>
 *@


                    @model DbFirstCRUD.Data.Entities.PaginatedDesignationViewModel

@{
    ViewData["Title"] = "Designation List";
}

<h1>Designation List</h1>

<!-- Table to display designations -->
<table class="table table-striped">
    <thead>
        <tr>
@*             <th>Designation ID</th>
 *@            <th>Designation Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Designations != null && Model.Designations.Count > 0)
        {
            foreach (var designation in Model.Designations)
            {
                <tr>
@*                     <td>@designation.DesignationId</td>
 *@                    <td>@designation.DesignationName</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@designation.DesignationId" class="btn btn-info">View</a>
                        <a asp-action="Edit" asp-route-id="@designation.DesignationId" class="btn btn-primary">Edit</a>
                        <a asp-action="Delete" asp-route-id="@designation.DesignationId" class="btn btn-danger"
                           onclick="return confirm('Are you sure you want to delete this designation?');">Delete</a>
                    </td>
                </tr>
            }
        }
      
    </tbody>
</table>


<a asp-action="Create" class="btn btn-success">Add New Designation</a>
<a href="@Url.Action("ExportToPdf", "Designation")" class="btn btn-danger">Export to PDF</a>

<form asp-action="Index" asp-route-pageSize="@Model.PageSize" method="get" class="form-inline mb-3">

    <label for="pageSize" class="mr-2">Show:</label>
    <select name="pageSize" class="form-control mr-2" onchange="this.form.submit()">
        <option value="10" selected="@(Model.PageSize == 10 ? "selected" : "")">10</option>
        <option value="20" selected="@(Model.PageSize == 20 ? "selected" : "")">20</option>
        <option value="30" selected="@(Model.PageSize == 30 ? "selected" : "")">30</option>
        <option value="40" selected="@(Model.PageSize == 40 ? "selected" : "")">40</option>
        <option value="50" selected="@(Model.PageSize == 50 ? "selected" : "")">50</option>
    </select>
    <span>rows per page</span>
</form>





<!-- Pagination Controls -->
<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center">
        <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
            <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.CurrentPage - 1)">Previous</a>
        </li>

        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                <a class="page-link" asp-action="Index" asp-route-pageNumber="@i">@i</a>
            </li>
        }

        <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
            <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.CurrentPage + 1)">Next</a>
        </li>
    </ul>
</nav>

<div class="text-center">
    <p>Showing page @Model.CurrentPage of @Model.TotalPages.</p>
</div>
